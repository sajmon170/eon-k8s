apiVersion: apps/v1
kind: Deployment
metadata:
  name: bootstrap-deployment
  labels:
      app: boostrap-node
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bootstrap-deployment
  template:
    metadata:
      labels:
        app: bootstrap-deployment
    spec:
      containers:
      - name: eon-client
        image: 10.1.1.18:5000/eon-client
        command: [
          "eon-client",
          "--stdout",
          "--log-level", "debug",
          "--bootstrap-mode",
          "--secret-key-seed", "1",
          "--script", "[!Wait {time: 2s}]"
        ]
        ports:
        - containerPort: 22137
