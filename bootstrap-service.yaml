apiVersion: v1
kind: Service
metadata:
  name: bootstrap-service
  labels:
    app: bootstrap-service
spec:
  type: ClusterIP
  clusterIP: 10.96.0.1
  ports:
  - port: 22137
    protocol: TCP
  selector:
    app: bootstrap-deployment
